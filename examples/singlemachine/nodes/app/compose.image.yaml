services:
  app-image:
    image: ${APP_IMAGE}:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - PUID=${PUID:-1000}
        - PGID=${PGID:-1000}
    profiles:
      - build
