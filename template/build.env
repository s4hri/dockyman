PROJECT_NAME=null
VERSION=v0.0.1
REGISTRY=iitschri
PROFILE=development
DOCKYMAN_VER=v1.2

BASE_DOCKER_SRC=alpine:latest # (ubuntu:latest, centos:latest)
BASE_NAME=${REGISTRY}/${PROJECT_NAME}-docker:${VERSION}