PROJECT_NAME=null
VERSION=v0.0.1
REGISTRY=iitschri
PROFILE=development
DOCKYMAN_VER=v1.5

PROJECT_DOCKER_SRC=ubuntu:latest # (ubuntu:latest, centos:latest)
PROJECT_DOCKER_NAME=${REGISTRY}/${PROJECT_NAME}-docker:${VERSION}
